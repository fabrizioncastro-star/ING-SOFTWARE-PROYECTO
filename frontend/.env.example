# Copiar a .env
# IP local de tu PC en la red WiFi (la misma red que el teléfono con Expo Go).
# NO usar localhost: el teléfono no puede resolverlo.
# Ver tu IP con: ipconfig (Windows) -> Dirección IPv4
EXPO_PUBLIC_API_URL=http://38.25.9.86